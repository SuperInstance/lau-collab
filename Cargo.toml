[package]
name = "lau-collab"
version = "0.1.0"
edition = "2021"
description = "Multiplayer collaboration layer for the Lau platform"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0.150"
